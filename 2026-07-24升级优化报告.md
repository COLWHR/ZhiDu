# 2026-07-24 升级优化报告

## 本轮目标

对 ZhiDo V1.2.1 做持续升级优化：优先修复真实失败点、降低测试触网和超时风险、提升前端构建表现，并用自动化测试反复验证。

## 已完成优化

### 1. 挂载健康检查接口

- 修改 `app/api/v1/api.py`，挂载已有的 `health.router`。
- 新增 `app/tests/test_e2e_network.py::test_health_endpoint`。
- 修复前：`GET /api/v1/health` 返回 404。
- 修复后：返回 `{"status": "ok", "message": "healthy"}`。

### 2. 阻止空参与者论坛启动

- 修改 `app/services/forum_service.py`。
- 新增 `app/tests/test_forum_creation.py::test_start_forum_without_participants_is_rejected`。
- 修复前：0 个参与者的论坛可以启动，调度器会空转并拖慢/卡住删除请求。
- 修复后：启动空论坛返回 400，错误信息包含 `participant`。

### 3. 稳定 Cypress 论坛冒烟测试

- 修改 `frontend/cypress/e2e/smoke.cy.ts`。
- 现在测试会先创建人格，再创建带参与者的论坛。
- 启动论坛时使用 `mock_llm` 和 `no_summary`，避免 E2E 测试访问真实大模型。
- 清理时先停止论坛，再删除论坛和测试人格。

### 4. 稳定实时上传/WebSocket E2E

- 修改 `frontend/cypress/e2e/_helpers.ts`，`startForum` 默认使用 mock LLM。
- 修改 `frontend/cypress/e2e/realtime_upload_ws.cy.ts`：
  - 先创建测试人格。
  - 创建带参与者的论坛。
  - 直接写消息时补充认证头。
  - 清理测试人格。

### 5. 修复论坛上传前后端契约不一致

- 修改 `frontend/src/views/ForumDetailView.vue`。
- 修复前：前端读取上传响应的 `response.data.url`，但后端上传接口返回 `storage_url` / `preview_url`，导致上传后发送聊天时 content 为空，后端返回 400。
- 修复后：优先读取 `preview_url`，其次 `storage_url`，保留 `url` 兼容旧响应。

### 6. 路由页面懒加载，降低首屏包体

- 修改 `frontend/src/router/index.ts`。
- 将 Home、Login、Register、Persona、ForumDetail、Assistant、SkillRepository、TimeGate 等页面改为路由级动态导入。
- 构建结果中主入口 JS 从约 `263.60 kB` 降到约 `97.52 kB`。
- 剩余大块主要来自 `antd-vendor`。

### 7. 修复 RealGodAgent 路由测试

- 修改 `app/agent/real_god.py`，调用 LLM 时传入 `route="nvwa_generate"`。
- 修改 `utils.py`，`get_chat_completion` 支持可选 `route` 参数并透传。
- 修复了 `app/tests/test_real_god_routing.py` 中的失败断言。

### 8. 去除覆盖测试中的真实 God 生成调用

- 修改 `app/tests/test_coverage_boost.py`。
- 使用本地 fake `RealGodAgent` 流式事件替代真实生成。
- 修复前：该文件会拖慢甚至导致分组测试超时。
- 修复后：`test_coverage_boost.py` 单文件 10 个测试通过。

## 验证结果

### 后端

```powershell
.\.venv312\Scripts\python.exe -m pytest app\tests tests -q
```

结果：

- `115 passed`
- `1 warning`
- 耗时约 `176.33s`

### 前端类型检查

```powershell
cd frontend
npm run type-check
```

结果：

- 通过
- `vue-tsc --noEmit` 无错误

### 前端单元测试

```powershell
cd frontend
npx vitest run
```

结果：

- `13 passed` 测试文件
- `35 passed` 测试

### 前端生产构建

```powershell
cd frontend
npm run build
```

结果：

- 构建通过
- 主入口 JS 降至约 `97.52 kB`
- 仍有 `antd-vendor` 超 500 kB 的提示

### Cypress 关键浏览器回归

```powershell
cd frontend
npx cypress run --browser electron --spec "cypress/e2e/auth_ui.cy.ts,cypress/e2e/smoke.cy.ts,cypress/e2e/realtime_upload_ws.cy.ts"
```

结果：

- `auth_ui.cy.ts` 通过
- `smoke.cy.ts` 通过
- `realtime_upload_ws.cy.ts` 通过
- 合计 `3 passing`

## 剩余风险与建议

1. `antd-vendor` 仍为约 `1.4 MB`，后续可评估按需组件、图标拆分或 UI 库替换成本。
2. 构建仍提示 `auth.ts` 与 `router/index.ts` 存在静态/动态混合导入关系，当前不影响构建和测试。
3. 后端全量测试仍需约 3 分钟，已经能跑完并全绿，但后续可以继续分类 `slow/integration/unit`。
4. 启动日志仍有 Redis `HELLO` 兼容提示，建议单独检查 Redis 服务版本或客户端协议配置。

## 总体结论

本轮升级后，项目健康检查、论坛启动保护、上传聊天链路、Cypress 冒烟稳定性、God 生成测试隔离、RealGod 路由行为和前端路由包体都有实质改善。

最关键的是：后端全量测试已从此前超时/失败状态变为 `115 passed`，前端类型检查、Vitest、构建和关键 Cypress 回归全部通过。
