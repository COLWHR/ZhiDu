# 项目启动指南

## 1. 项目概览

ZhiDo / MADF 是一个多智能体圆桌与对话平台。当前仓库包含前端 Vue 3 应用和后端 FastAPI 服务。

## 2. 启动前准备

- Python 3.10+
- Node.js 18+
- 已配置可用的 `API_KEY`

建议先在项目根目录准备 `.env`：

```ini
API_KEY=your_llm_api_key
MODEL_NAME=glm-4.5
BASE_URL=https://open.bigmodel.cn/api/paas/v4/
SECRET_KEY=your_secret_key
DATABASE_URL=file:madf.db
REDIS_URL=redis://localhost:6379/0
```

## 3. 本地开发启动

### 3.1 启动后端

```bash
python -m venv .venv
.venv\Scripts\activate
pip install -r requirements.txt
uvicorn app.main:app --reload --host 0.0.0.0 --port 8000
```

后端地址：

- API：`http://localhost:8000/api/v1`
- 文档：`http://localhost:8000/docs`

### 3.2 启动前端

```bash
cd frontend
npm install
npm run dev
```

前端地址：

- 页面：`http://localhost:5173`

## 4. Docker 启动

如果希望一键启动，可以在项目根目录执行：

```bash
docker compose up -d --build
```

## 5. 常用检查

```bash
# 后端测试
python -m pytest

# 前端类型检查
cd frontend
npm run type-check

# 浏览器回归
npx cypress run --browser electron --spec 'cypress/e2e/time_gate_smoke.cy.ts'
```

## 6. 常见问题

- 如果 `8000` 端口被占用，先确认是否有旧的 Uvicorn 实例在运行
- 如果模型接口返回 401，优先检查 `.env` 中的 `API_KEY`
- 如果图片上传后无法发送，确认后端服务和前端访问的是同一套环境

