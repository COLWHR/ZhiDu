# 代码变更与布网说明

本文档用于说明本项目的日常改动、构建与上线流程。

## 1. 本地修改流程

1. 拉取最新代码
2. 修改前端或后端代码
3. 本地自测
4. 再提交到测试环境或生产环境

## 2. 提交前检查

- 前端：`cd frontend && npm run type-check`
- 后端：`python -m pytest`
- 浏览器回归：`npx cypress run --browser electron --spec 'cypress/e2e/time_gate_smoke.cy.ts'`
- 检查 `.env` 中的 `API_KEY`、`SECRET_KEY`、`MODEL_NAME`、`BASE_URL`

## 3. 构建与发布

### 3.1 Docker 发布

```bash
docker compose up -d --build
docker compose ps
docker compose logs -f
```

### 3.2 手工发布

- 后端启动：`uvicorn app.main:app --host 0.0.0.0 --port 8000`
- 前端启动：`cd frontend && npm run dev`

## 4. 变更后验证

- 首页是否能正常打开
- 登录是否正常
- 时空之门是否能发送消息
- 图片上传是否能插入消息
- 清空对话是否能恢复欢迎状态

## 5. 回滚建议

如果上线后出现问题：

1. 先停止当前容器或进程
2. 恢复上一版镜像或上一版代码
3. 重新检查 `.env`
4. 重新跑一遍核心回归用例

## 6. 常见风险

- 不要把真实密钥提交到仓库
- 不要在生产环境使用默认 `SECRET_KEY`
- 不要让前端和后端指向不同的后端地址

