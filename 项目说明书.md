# ZhiDo / MADF 项目说明书

## 1. 项目概述

ZhiDo / MADF 是一个面向多智能体圆桌讨论、角色生成与智能对话的 Web 平台。项目采用前后端分离架构，前端基于 Vue 3 + Vite 构建交互界面，后端基于 FastAPI 提供 REST API、WebSocket 实时推送、用户认证、数据持久化与大模型调用能力。

平台的核心目标是让用户能够快速创建专家型智能体，围绕指定主题组织圆桌讨论，并通过实时消息流观察主持人、嘉宾智能体和用户之间的互动过程。同时，系统提供“时空之门”单聊模式，支持用户选择某个智能体进行持续对话，并保存历史消息。

## 2. 项目定位

本项目适合以下场景：

- 多智能体圆桌会议、头脑风暴、专家访谈和观点碰撞。
- 基于主题自动生成专家角色，并将角色保存为可复用智能体。
- 用户与单个智能体进行流式对话，支持图片参与上下文。
- 小团队或个人在本地、Docker 或轻量云服务器上部署 AI 对话应用。
- 对多智能体讨论流程、主持人调度、发言队列和上下文记忆机制进行实验。

## 3. 核心功能

| 模块 | 功能说明 |
| --- | --- |
| 用户认证 | 支持注册、登录、JWT 鉴权和当前用户信息获取。 |
| 智能体管理 | 支持创建、查询、编辑、删除智能体角色；角色包含姓名、头衔、简介、理论体系、立场和系统提示词。 |
| AI 角色生成 | 通过 RealGodAgent 根据用户输入生成专家角色，支持流式返回生成进度，并自动保存角色。 |
| 主持人管理 | 支持创建、查询和删除主持人，主持人可用于论坛开场、总结和收束讨论。 |
| 圆桌论坛 | 支持创建论坛、绑定嘉宾和主持人、启动/停止讨论、删除论坛、查询消息和系统日志。 |
| 实时讨论 | 通过 WebSocket 推送发言片段、完整消息、系统日志和论坛状态，支持类似直播的讨论体验。 |
| 观众插话 | 用户可以向运行中的论坛注入消息，系统会重新组织后续发言逻辑。 |
| 时空之门 | 支持用户与单个智能体进行流式单聊，保存、读取和清空对话历史。 |
| 图片上传 | 支持图片上传，返回 `/uploads/...` 地址；单聊接口可将图片 URL 纳入大模型上下文。 |
| 系统日志 | 记录论坛运行过程中的系统事件、发言状态、异常和调度信息。 |
| 测试与回归 | 后端使用 Pytest，前端使用 Vitest、Cypress，并包含多条核心流程回归用例。 |

## 4. 技术架构

### 4.1 整体架构

```text
用户浏览器
  |
  | HTTP / WebSocket
  v
Vue 3 前端
  |-- Vue Router：页面路由
  |-- Pinia：认证、智能体、论坛和配置状态
  |-- Ant Design Vue：基础 UI 组件
  |
  | REST API / WebSocket
  v
FastAPI 后端
  |-- API 路由：认证、用户、智能体、论坛、主持人、文件上传
  |-- ForumService：论坛业务编排
  |-- ForumScheduler：圆桌讨论调度、发言队列、实时推送
  |-- Agent 层：主持人、嘉宾、角色生成智能体
  |-- WebSocket Manager：论坛级实时广播
  |
  | 数据读写 / 缓存 / 外部调用
  v
SQLite / PostgreSQL / Redis / LLM API / 搜索服务
```

### 4.2 前端技术栈

- Vue 3：页面和组件开发。
- TypeScript：提升类型安全和可维护性。
- Vite：开发服务器和生产构建。
- Pinia：管理登录态、论坛、智能体、配置等前端状态。
- Vue Router：管理登录页、首页、智能体页、论坛页、时空之门等路由。
- Ant Design Vue：提供表单、按钮、列表、弹窗等 UI 组件。
- Axios：统一封装 API 请求、Token 注入、错误处理和 GET 请求缓存。
- Vitest / Cypress：分别用于单元测试和浏览器端回归测试。

主要页面：

| 路由 | 页面 | 说明 |
| --- | --- | --- |
| `/auth/login` | 登录页 | 用户登录并保存 Token。 |
| `/auth/register` | 注册页 | 创建新用户。 |
| `/dashboard` | 首页 | 项目入口和核心功能概览。 |
| `/personas` | 智能体管理 | 创建、查看和维护智能体。 |
| `/forums` | 论坛列表 | 创建和管理圆桌论坛。 |
| `/forums/:id` | 论坛详情 | 查看讨论、实时消息、日志和控制讨论状态。 |
| `/assistants` | AI 角色生成 | 调用 RealGodAgent 生成智能体。 |
| `/time-gate` | 时空之门 | 与指定智能体进行单聊。 |

### 4.3 后端技术栈

- FastAPI：提供 API 服务、自动生成接口文档和请求参数校验。
- Uvicorn：ASGI 服务运行器。
- Pydantic / pydantic-settings：数据模型和环境配置。
- libsql-client：默认连接本地 SQLite，也支持 Turso 等 LibSQL 远程数据库。
- psycopg2：保留 PostgreSQL 连接能力。
- Redis：可选缓存和系统日志缓冲，连接失败时系统会降级运行。
- python-jose / passlib：JWT 和密码哈希。
- ZhipuAI SDK / HTTPX / Requests：接入外部大模型与网络请求。

## 5. 后端接口概览

后端 API 默认前缀为 `/api/v1`。

| 模块 | 主要接口 | 说明 |
| --- | --- | --- |
| 认证 | `POST /auth/login`、`POST /auth/register` | 登录、注册。 |
| 用户 | `GET /users/me`、`GET /users/{username}` | 查询当前用户和指定用户。 |
| 智能体 | `POST /personas`、`GET /personas`、`PUT /personas/{id}`、`DELETE /personas/{id}` | 智能体 CRUD。 |
| 主持人 | `GET /moderators`、`POST /moderators`、`DELETE /moderators/{id}` | 主持人管理。 |
| 论坛 | `POST /forums`、`GET /forums`、`POST /forums/{id}/start`、`POST /forums/{id}/stop` | 论坛创建、查询和控制。 |
| 论坛消息 | `GET /forums/{id}/messages`、`POST /forums/{id}/messages`、`POST /forums/{id}/chat` | 查询消息、发送消息、观众插话。 |
| 论坛日志 | `GET /forums/{id}/logs` | 查询系统日志。 |
| 实时通信 | `WS /forums/{id}/ws` | 论坛级 WebSocket 实时推送。 |
| 单聊 | `POST /agents/chat/stream`、`GET /agents/chat/history/{persona_id}`、`DELETE /agents/chat/history/{persona_id}` | 时空之门流式对话和历史记录。 |
| 角色生成 | `POST /god/generate_real`、`POST /god/generate` | AI 生成角色，兼容旧接口。 |
| 上传 | `POST /upload/image` | 上传图片并返回可访问 URL。 |

## 6. 数据模型

系统默认使用 SQLite 文件 `madf.db`，生产环境可通过环境变量切换到 PostgreSQL 或 LibSQL/Turso。

核心数据表如下：

| 表名 | 说明 |
| --- | --- |
| `users` | 用户账号、密码哈希、角色和创建时间。 |
| `personas` | 智能体角色信息，包括简介、理论、立场和系统提示词。 |
| `moderators` | 主持人角色、开场模板、总结模板和创建者。 |
| `forums` | 圆桌论坛主题、创建者、主持人、状态、时长和总结历史。 |
| `forum_participants` | 论坛与智能体的多对多关系，并保存私有思考历史。 |
| `messages` | 论坛消息、发言人、内容、思考内容和时间戳。 |
| `observations` | 用户进入和离开论坛的观察记录。 |
| `system_logs` | 论坛运行中的系统日志。 |
| `chat_messages` | 时空之门单聊历史。 |
| `god_logs` | 高权限用户或系统智能体操作日志。 |

## 7. 关键流程

### 7.1 用户登录

1. 用户在前端提交用户名和密码。
2. 后端校验密码哈希，通过后签发 JWT。
3. 前端将 Token 保存到本地，并在后续请求中注入 `Authorization: Bearer <token>`。
4. 需要登录的页面由前端路由守卫保护，后端接口由依赖注入校验当前用户。

### 7.2 AI 生成智能体

1. 用户在 AI 角色生成页面输入需求，例如“生成 3 位研究人工智能伦理的专家”。
2. 前端调用 `/api/v1/god/generate_real`。
3. RealGodAgent 结合模型能力和可选搜索能力生成角色。
4. 后端通过 SSE 持续返回生成状态。
5. 生成结果经统一服务保存到 `personas` 表，并刷新相关缓存。

### 7.3 圆桌论坛讨论

1. 用户创建论坛，选择主题、参会智能体和主持人。
2. 用户启动论坛，后端将论坛状态切换为 `running`。
3. ForumScheduler 创建运行任务，主持人先进行开场。
4. 系统根据共享记忆、私有记忆、发言队列和上文内容选择下一位嘉宾。
5. 嘉宾调用大模型生成发言，后端通过 WebSocket 推送流式片段和完整消息。
6. 用户插话时，消息进入队列，系统重新组织后续发言。
7. 论坛到达时长或用户手动停止后，主持人进行收束，论坛状态结束。

### 7.4 时空之门单聊

1. 用户选择某个智能体并输入消息。
2. 前端调用 `/api/v1/agents/chat/stream` 获取流式回复。
3. 后端根据智能体系统提示词、历史上下文和图片内容组装大模型消息。
4. 回复以 `text/event-stream` 返回前端。
5. 对话历史通过 `/agents/chat/message` 保存，可按智能体读取或清空。

## 8. 运行与部署

### 8.1 环境要求

- Python 3.10+
- Node.js 18+
- Docker 和 Docker Compose（可选）
- 可用的大模型 API Key

### 8.2 关键环境变量

```ini
API_KEY=your_llm_api_key
MODEL_NAME=glm-4.5
BASE_URL=https://open.bigmodel.cn/api/paas/v4/
SECRET_KEY=your_secret_key
DATABASE_URL=file:madf.db
REDIS_URL=redis://localhost:6379/0
```

可选变量：

```ini
ENVIRONMENT=development
ZHIPUAI_API_KEY=your_zhipu_key
SERPAPI_API_KEY=your_serpapi_key
VOLC_API_KEY=your_volc_key
VOLC_VISION_MODEL=doubao-vision-pro-12k
VOLC_BASE_URL=https://aquasearch.volces.com/api/v3/
TURSO_DATABASE_URL=libsql://your-db.turso.io
TURSO_AUTH_TOKEN=your_turso_token
SECRET_KEY_FILE=/path/to/secret_key
```

说明：

- `API_KEY` 用于调用外部大模型服务，生产环境必须设置。
- `MODEL_NAME` 默认回退为 `glm-4.5`。
- `BASE_URL` 默认指向智谱开放平台。
- `SECRET_KEY` 用于签发和校验 JWT，生产环境必须使用高强度随机值。
- `DATABASE_URL` 默认为 `file:madf.db`，Docker 中默认为 `file:/app/data/madf.db`。
- `REDIS_URL` 可用时用于缓存和日志缓冲，不可用时系统会降级到直接数据库写入。

### 8.3 本地开发启动

后端：

```bash
python -m venv .venv
.venv\Scripts\activate
pip install -r requirements.txt
uvicorn app.main:app --reload --host 0.0.0.0 --port 8000
```

前端：

```bash
cd frontend
npm install
npm run dev
```

访问地址：

- 前端：`http://localhost:5173`
- 后端 API：`http://localhost:8000/api/v1`
- 后端文档：`http://localhost:8000/docs`

### 8.4 Docker 部署

```bash
docker compose up -d --build
docker compose ps
docker compose logs -f
```

Docker 镜像会先构建前端静态文件，再把构建产物和 FastAPI 后端打包到同一个 Python 镜像中。容器启动时会同时启动 Redis 和 Uvicorn，并通过 `8000` 端口对外提供前端页面和 API 服务。

生产环境建议：

- 使用 Nginx 反向代理并启用 HTTPS。
- 将 `madf_data` 卷定期备份。
- 不要把 `.env`、数据库文件、上传文件和真实密钥提交到仓库。
- 设置强随机 `SECRET_KEY`，不要使用默认值。

## 9. 测试与质量保障

常用检查命令：

```bash
# 后端测试
python -m pytest

# 前端类型检查
cd frontend
npm run type-check

# 前端单元测试
npm run test:unit

# 浏览器回归
npx cypress run --browser electron --spec "cypress/e2e/time_gate_smoke.cy.ts"
```

测试覆盖重点：

- 认证、用户信息和权限控制。
- 智能体、主持人和论坛的 CRUD。
- 论坛启动、停止、消息查询和系统日志。
- WebSocket 实时推送和前端页面回归。
- 时空之门流式对话、图片上下文和历史记录。
- 大模型接口异常、空流、超时和网络错误的降级提示。

## 10. 安全与权限

- 后端使用 JWT 保护核心接口。
- 论坛访问按创建者隔离，管理员或 `god` 角色拥有更高权限。
- 密码使用哈希存储，不保存明文密码。
- 上传文件存放于 `uploads/`，需要保证目录可写并控制公开访问范围。
- 生产环境必须设置 `SECRET_KEY`，否则后端会拒绝使用不安全默认值。

当前需要注意的安全边界：

- CORS 当前允许所有来源，生产环境建议收紧为可信域名。
- 全局异常处理会返回较详细错误信息，生产环境建议减少对外暴露的调试细节。
- 上传接口当前主要面向图片使用场景，生产环境建议增加文件类型、大小和内容安全校验。

## 11. 项目结构

```text
app/
  api/                 FastAPI 路由和依赖
  agent/               主持人、嘉宾、角色生成和记忆逻辑
  core/                配置、安全、缓存、响应、WebSocket 和工具能力
  crud/                数据读写逻辑
  db/                  数据库连接、SQLite schema 和 PostgreSQL schema
  services/            论坛、角色等业务服务
  tests/               后端测试
frontend/
  src/
    components/        业务组件
    views/             页面视图
    stores/            Pinia 状态
    router/            前端路由
    utils/             请求封装
  cypress/             E2E 测试
docs/
  adr/                 架构决策记录
  architecture.mmd     架构图
uploads/               上传文件目录
requirements.txt       后端依赖
docker-compose.yml     Docker Compose 配置
Dockerfile             前后端一体化镜像构建
```

## 12. 当前优势

- 架构清晰：前端、后端、Agent、服务层、数据层边界相对明确。
- 部署轻量：默认 SQLite + 单容器即可运行，适合本地演示和轻量云服务器。
- 实时体验完整：论坛讨论通过 WebSocket 实现流式片段、完整消息和系统日志推送。
- 扩展空间充足：已预留 PostgreSQL、Turso、Redis、搜索服务和视觉模型配置。
- 回归意识较强：仓库内已有后端测试、前端单测和 Cypress 场景测试。

## 13. 当前风险与优化建议

| 风险点 | 影响 | 建议 |
| --- | --- | --- |
| CORS 过宽 | 生产环境可能被非可信来源调用 | 按部署域名配置允许来源。 |
| 上传校验较弱 | 可能上传非预期文件或过大文件 | 增加 MIME、扩展名、大小和安全扫描限制。 |
| SQLite 单机写入能力有限 | 高并发论坛可能出现写锁压力 | 小规模继续使用 SQLite；中高并发迁移 PostgreSQL。 |
| Redis 与应用同容器 | 扩容和运维边界不够清晰 | 生产环境建议拆分独立 Redis 服务。 |
| 外部模型依赖强 | API Key、额度、网络和模型权限会影响体验 | 完善错误提示、重试、降级和监控告警。 |
| 调试信息暴露 | 生产环境可能泄露内部异常细节 | 按 `ENVIRONMENT` 区分开发和生产错误响应。 |

## 14. 后续演进方向

- 增加后台管理页，支持用户、角色、论坛和系统日志统一管理。
- 增加模型供应商配置页，支持多模型切换和调用额度提示。
- 为论坛讨论增加模板，例如“专家评审”“商业圆桌”“论文答辩”“产品复盘”。
- 增加文件库或知识库，让智能体讨论可以引用用户上传资料。
- 完善审计日志和导出功能，支持导出讨论纪要、发言记录和角色卡。
- 为生产部署补充数据库迁移、备份恢复、健康检查和监控告警。

## 15. 结论

ZhiDo / MADF 当前已经具备一个多智能体圆桌平台的核心闭环：用户认证、智能体管理、角色生成、主持人机制、论坛调度、实时推送、单聊对话、图片上传和基础测试体系均已落地。项目适合继续作为 AI 圆桌讨论产品原型、内部知识研讨工具或多智能体实验平台推进。

下一阶段建议优先加强生产安全配置、上传校验、部署监控和数据库迁移能力，让系统从“可演示、可本地部署”进一步走向“可长期运行、可多人使用”。
